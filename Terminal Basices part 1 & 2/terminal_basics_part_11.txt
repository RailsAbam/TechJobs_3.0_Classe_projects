<h1>What does the man command do?:</h1>
<p>It ask us what manual page we want</p>
<h2> Type in man rm. How do you scroll and get out?</h2>
<p> I use the (^)to scroll and (q) to get out and end process</p>

<h3>How do you jump between words in the terminal?</h3>
<p>Hold option ( alt on some keyboards) and press the arrow keys left or right to move by word.</p>

<h1>How do you get to the end of a line in terminal?</h1>
<p>To navigate to the beginning of the line in use: “CTRL+a”. To navigate to the end of the line in use: “CTRL+e”.</p>

<h2></h2>
<p>Out of the box you can use the quite bizarre Esc + F to move to the beginning of the next word and Esc + B to move to the beginning of th</p>

<h1>How do you delete a word (without pressing backspace multiple times) in terminal?</h1>
<p>Ctrl+D or Delete – remove or deletes the character under the cursor.</p>

<h3>What is the difference between a terminal and shell?</h3>
<p>Terminal is an application that gives us a command line interface (or CLI) to interact with the computer. Everything you can do in Finder you can do in Terminal. While he shell is the program which actually processes commands and returns output</p>

<h4>What is an absolute path?</h4>
<p> absolute, starting from the root (first /, then Users, then eschoppik, then Desktop</p>

<h5>What is an relative path?</h5>
<p>cd Desktop - relative to where I am currently</p>

<h3>What is a flag? Give three examples of flags you have used.</h3>
<p>re a way to set options and pass in arguments to the commands you run.</p>
<p>ls</p>
<p> ~ </p>
<p>ls *</p>

<h2>What do the r and f flags do with the rm command?</h2>
<p>For example if a file is unwritable, rm will prompt you whether to remove that file or not, to avoid this and simply execute the operation. When you combine the -r and -f flags, it means that recursively and forcibly remove a directory</p>
